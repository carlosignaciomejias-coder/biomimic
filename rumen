<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simulador Biomimético: Ecología Microbiana y Fisiología Ruminal</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #0f172a;
            color: #e2e8f0;
        }
        canvas {
            background: radial-gradient(circle at 50% 50%, #1e293b 0%, #0f172a 100%);
            border: 2px solid #334155;
            border-radius: 12px;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.5);
            width: 100%;
            height: auto;
            max-width: 850px;
        }
        .stat-card {
            background-color: rgba(30, 41, 59, 0.7);
            backdrop-filter: blur(8px);
            border: 1px solid #334155;
        }
    </style>
</head>
<body class="p-3 md:p-8 min-h-screen">

    <div class="max-w-7xl mx-auto space-y-4 md:space-y-6">
        <!-- Encabezado -->
        <header class="flex flex-col md:flex-row justify-between items-start md:items-center bg-slate-800/80 p-4 md:p-5 rounded-xl border border-slate-700 shadow-lg gap-3">
            <div>
                <h1 class="text-xl md:text-3xl font-bold text-emerald-400 flex items-center gap-2">
                    <svg class="w-7 h-7 md:w-8 md:h-8 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"></path></svg>
                    Simulador Ruminal Biomimético
                </h1>
                <p class="text-slate-400 text-xs md:text-sm mt-1">Modelación Dinámica Nutricional: Perfiles de AGVs, Lactato, Biohidrogenación Lipídica y Cinética de Gases.</p>
            </div>
            <div class="self-end md:self-auto">
                <span id="statusBadge" class="px-3 py-1.5 md:px-4 md:py-2 rounded-full text-xs font-semibold bg-emerald-500/20 text-emerald-300 border border-emerald-500/30">
                    Estado: Fisiológico Normal
                </span>
            </div>
        </header>

        <!-- Área Principal Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-4 md:gap-6">
            
            <!-- Canvas de Simulación (8 Cols) -->
            <div class="lg:col-span-8 space-y-4">
                <div class="relative flex justify-center">
                    <canvas id="simCanvas" width="850" height="520" class="cursor-crosshair"></canvas>
                    <!-- Leyenda Adaptativa con Estratificación y Formas -->
                    <div class="absolute top-2 left-2 md:top-4 md:left-4 bg-slate-900/90 p-2 md:p-3 rounded-lg border border-slate-700/60 text-[10px] md:text-xs space-y-2 backdrop-blur shadow-xl max-w-[210px] z-10">
                        <div class="font-semibold text-slate-200 border-b border-slate-700/60 pb-1">Estratos del Rumen</div>
                        <div class="flex items-center gap-2 text-sky-300"><span class="w-2.5 h-2.5 rounded-sm bg-sky-500/40 border border-sky-400 inline-block shrink-0"></span> 1. Bóveda de Gas</div>
                        <div class="flex items-center gap-2 text-emerald-300"><span class="w-2.5 h-2.5 rounded-sm bg-emerald-600/50 border border-emerald-400 inline-block shrink-0"></span> 2. Balsa de Fibra / Mat</div>
                        <div class="flex items-center gap-2 text-amber-300"><span class="w-2.5 h-2.5 rounded-sm bg-amber-600/40 border border-amber-400 inline-block shrink-0"></span> 3. Zona Transición</div>
                        <div class="flex items-center gap-2 text-amber-500"><span class="w-2.5 h-2.5 rounded-sm bg-amber-900/60 border border-amber-600 inline-block shrink-0"></span> 4. Fase Líquida</div>

                        <div class="font-semibold text-slate-200 border-b border-slate-700/60 pb-1 pt-1">Microbios (Seres Vivos)</div>
                        <div class="flex items-center gap-2">
                            <span class="w-3 h-3 rounded-full bg-emerald-400 border border-emerald-600 inline-block shrink-0"></span>
                            Celulolíticas (<span class="font-mono text-emerald-300">Cocos</span>)
                        </div>
                        <div class="flex items-center gap-2">
                            <span class="w-4 h-2 rounded-full bg-amber-400 inline-block shrink-0"></span>
                            Amilolíticas (<span class="font-mono text-amber-300">Bacilos</span>)
                        </div>
                        <div class="flex items-center gap-2">
                            <span class="w-3 h-3 rounded-full bg-purple-400 inline-block shrink-0 relative">
                                <span class="absolute -inset-1 rounded-full border border-yellow-300/50 animate-pulse"></span>
                            </span>
                            Metanógenas (+<span class="text-yellow-300">CH₄ Halo</span>)
                        </div>
                        
                        <div class="font-semibold text-slate-200 border-b border-slate-700/60 pb-1 pt-1">Metabolitos (AGVs)</div>
                        <div class="flex items-center gap-2"><span class="w-2.5 h-2.5 bg-emerald-500 inline-block shrink-0"></span> Acetato (<span class="font-mono">Cuadrado</span>)</div>
                        <div class="flex items-center gap-2"><span class="w-2.5 h-2.5 bg-amber-400 inline-block shrink-0"></span> Propionato</div>
                        <div class="flex items-center gap-2"><span class="w-2.5 h-2.5 bg-indigo-400 inline-block shrink-0"></span> Butirato</div>
                        <div class="flex items-center gap-2"><span class="w-2.5 h-2.5 bg-rose-500 inline-block shrink-0"></span> Lactato</div>
                    </div>
                </div>

                <!-- Controles Interactivos -->
                <div class="grid grid-cols-1 sm:grid-cols-3 gap-3 md:gap-4">
                    <div class="stat-card p-3 md:p-4 rounded-xl space-y-2 md:space-y-3">
                        <h3 class="text-xs md:text-sm font-semibold text-slate-300">1. Configurar Dieta</h3>
                        <select id="dietSelect" class="w-full bg-slate-900 border border-slate-700 rounded-lg p-2 text-xs md:text-sm text-emerald-400 focus:outline-none focus:border-emerald-500">
                            <option value="forage_mature">1. Forraje Maduro / Lignificado (Alta Fibra)</option>
                            <option value="forage_young">2. Forraje Tierno / Leguminosas</option>
                            <option value="mixed" selected>3. Dieta Balanceada (50/50)</option>
                            <option value="concentrate_cereal">4. Alta en Grano / Cereal (Riesgo SARA)</option>
                            <option value="concentrate_acidosis">5. Sobrecarga de Granos (Acidosis Láctica)</option>
                            <option value="diet_lipids">6. Suplementada con Lípidos / Aceites</option>
                            <option value="diet_npn">7. Alta en NPN (Urea) + Melaza</option>
                        </select>
                        <button id="feedBtn" class="w-full bg-emerald-600 hover:bg-emerald-500 text-white font-semibold py-2 px-3 rounded-lg transition text-xs md:text-sm flex items-center justify-center gap-2 shadow-lg">
                            Suministrar Bolo Alimenticio
                        </button>
                    </div>

                    <div class="stat-card p-3 md:p-4 rounded-xl space-y-2 md:space-y-3">
                        <h3 class="text-xs md:text-sm font-semibold text-slate-300">2. Fisiología y Rumia</h3>
                        <button id="ruminateBtn" class="w-full bg-amber-600 hover:bg-amber-500 text-white font-semibold py-2 px-3 rounded-lg transition text-xs md:text-sm flex items-center justify-center gap-2">
                            Estimular Ciclo de Rumia
                        </button>
                        <button id="bufferBtn" class="w-full bg-cyan-700 hover:bg-cyan-600 text-white font-semibold py-2 px-3 rounded-lg transition text-xs md:text-sm flex items-center justify-center gap-2">
                            Añadir Bicarbonato / Saliva
                        </button>
                    </div>

                    <div class="stat-card p-3 md:p-4 rounded-xl space-y-2 md:space-y-3">
                        <h3 class="text-xs md:text-sm font-semibold text-slate-300">3. Modulación</h3>
                        <button id="saponinBtn" class="w-full bg-purple-700 hover:bg-purple-600 text-white font-semibold py-2 px-3 rounded-lg transition text-xs md:text-sm flex items-center justify-center gap-2">
                            Agregar Taninos / Saponinas
                        </button>
                        <button id="resetBtn" class="w-full bg-slate-700 hover:bg-slate-600 text-slate-200 font-semibold py-2 px-3 rounded-lg transition text-xs md:text-sm">
                            Reiniciar Sistema
                        </button>
                    </div>
                </div>
            </div>

            <!-- Panel de Monitoreo Fisiológico (4 Cols) -->
            <div class="lg:col-span-4 space-y-3 md:space-y-4">
                
                <!-- Medidor de pH -->
                <div class="stat-card p-4 md:p-5 rounded-xl space-y-2 md:space-y-3">
                    <div class="flex justify-between items-center">
                        <span class="text-xs md:text-sm font-medium text-slate-400">pH Ruminal</span>
                        <span id="phValue" class="text-xl md:text-2xl font-bold text-emerald-400">6.65</span>
                    </div>
                    <div class="w-full bg-slate-700 h-2.5 md:h-3 rounded-full overflow-hidden relative">
                        <div id="phBar" class="bg-emerald-500 h-full transition-all duration-300" style="width: 75%;"></div>
                    </div>
                    <div class="flex justify-between text-[10px] text-slate-400 font-mono">
                        <span>5.0 (Acidosis)</span>
                        <span>6.2 (Óptimo)</span>
                        <span>7.2 (Alcalino)</span>
                    </div>
                </div>

                <!-- Perfil AGVs y Lactato -->
                <div class="stat-card p-4 md:p-5 rounded-xl space-y-2 md:space-y-3">
                    <h3 class="text-xs md:text-sm font-semibold text-slate-300 border-b border-slate-700 pb-2">Perfil de AGVs y Lactato</h3>
                    <div class="space-y-2.5 text-xs">
                        <div>
                            <div class="flex justify-between mb-1">
                                <span class="text-emerald-400 font-semibold flex items-center gap-1.5">
                                    <span class="w-2.5 h-2.5 bg-emerald-500 inline-block"></span>
                                    Acetato (Fibra)
                                </span>
                                <span id="acetatePct" class="font-bold text-emerald-300">62%</span>
                            </div>
                            <div class="w-full bg-slate-700 h-2 rounded-full overflow-hidden"><div id="acetateBar" class="bg-emerald-500 h-full transition-all duration-300" style="width: 62%;"></div></div>
                        </div>
                        <div>
                            <div class="flex justify-between mb-1">
                                <span class="text-amber-400 font-semibold flex items-center gap-1.5">
                                    <span class="w-2.5 h-2.5 bg-amber-400 inline-block"></span>
                                    Propionato (Almidón)
                                </span>
                                <span id="propionatePct" class="font-bold text-amber-300">26%</span>
                            </div>
                            <div class="w-full bg-slate-700 h-2 rounded-full overflow-hidden"><div id="propionateBar" class="bg-amber-400 h-full transition-all duration-300" style="width: 26%;"></div></div>
                        </div>
                        <div>
                            <div class="flex justify-between mb-1">
                                <span class="text-indigo-400 font-semibold flex items-center gap-1.5">
                                    <span class="w-2.5 h-2.5 bg-indigo-400 inline-block"></span>
                                    Butirato (Epitelio)
                                </span>
                                <span id="butyratePct" class="font-bold text-indigo-300">12%</span>
                            </div>
                            <div class="w-full bg-slate-700 h-2 rounded-full overflow-hidden"><div id="butyrateBar" class="bg-indigo-400 h-full transition-all duration-300" style="width: 12%;"></div></div>
                        </div>
                        <div>
                            <div class="flex justify-between mb-1">
                                <span class="text-rose-400 font-semibold flex items-center gap-1.5">
                                    <span class="w-2.5 h-2.5 bg-rose-500 inline-block"></span>
                                    Lactato (Acidosis)
                                </span>
                                <span id="lactatePct" class="font-bold text-rose-300">0%</span>
                            </div>
                            <div class="w-full bg-slate-700 h-2 rounded-full overflow-hidden"><div id="lactateBar" class="bg-rose-500 h-full transition-all duration-300" style="width: 0%;"></div></div>
                        </div>
                    </div>
                </div>

                <!-- Emisiones de Gas (Modelo Fisiológico Estequiométrico) -->
                <div class="stat-card p-4 md:p-5 rounded-xl space-y-2 md:space-y-3">
                    <div class="flex justify-between items-center border-b border-slate-700 pb-2">
                        <h3 class="text-xs md:text-sm font-semibold text-slate-300">Cinética de Gases Ruminales</h3>
                        <span class="text-[10px] text-slate-400 font-mono">Modelo Estequiométrico</span>
                    </div>
                    <div class="grid grid-cols-2 gap-3 text-center">
                        <div class="bg-slate-900/60 p-2.5 rounded-lg border border-slate-700/50 space-y-1">
                            <span class="text-[10px] text-slate-400 block">Metano (CH₄)</span>
                            <span id="ch4RateDisplay" class="text-base md:text-lg font-bold text-amber-400">280 L/día</span>
                            <span id="ch4TotalDisplay" class="text-[10px] text-slate-500 block">Acumulado: 0.0 L</span>
                        </div>
                        <div class="bg-slate-900/60 p-2.5 rounded-lg border border-slate-700/50 space-y-1">
                            <span class="text-[10px] text-slate-400 block">Dióxido (CO₂)</span>
                            <span id="co2RateDisplay" class="text-base md:text-lg font-bold text-sky-400">540 L/día</span>
                            <span id="co2TotalDisplay" class="text-[10px] text-slate-500 block">Acumulado: 0.0 L</span>
                        </div>
                    </div>
                </div>

                <!-- Dinámica Microbiana -->
                <div class="stat-card p-4 md:p-5 rounded-xl space-y-2 md:space-y-3">
                    <h3 class="text-xs md:text-sm font-semibold text-slate-300 border-b border-slate-700 pb-2">Población Microbiana</h3>
                    <div class="text-xs space-y-2">
                        <div class="flex justify-between items-center">
                            <span class="text-slate-400">Celulolíticas:</span>
                            <span id="cellulolyticCount" class="font-mono font-bold text-emerald-400">Dominante</span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-slate-400">Amilolíticas:</span>
                            <span id="amylolyticCount" class="font-mono font-bold text-amber-400">Moderada</span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-slate-400">Metanógenos:</span>
                            <span id="methanogenStatus" class="font-mono font-bold text-purple-400">Activas</span>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <!-- Script de Simulación Biomimética Estratificada con Cinética de Gases Estequiométrica -->
    <script>
        const canvas = document.getElementById('simCanvas');
        const ctx = canvas.getContext('2d');

        const state = {
            diet: 'mixed',
            ph: 6.65,
            acetate: 62,
            propionate: 26,
            butyrate: 12,
            lactate: 0,
            
            ch4RateLDay: 280,
            co2RateLDay: 540,
            
            ch4AccumulatedL: 0,
            co2AccumulatedL: 0,
            
            saponinActive: false,
            saponinTimer: 0,
            salivationBuffer: 0,
            co2BufferSurge: 0,
            acidosis: false,
            severeAcidosis: false,
            particles: [],
            microbes: [],
            vfaParticles: [],
            bubbles: []
        };

        const anatomy = {
            center: { x: 520, y: 260 },
            
            gasCap: { yTop: 50, yBottom: 130, rx: 200 },
            fiberMat: { yTop: 130, yBottom: 230, rx: 230 },
            transition: { yTop: 230, yBottom: 330, rx: 245 },
            liquidZone: { yTop: 330, yBottom: 450, rx: 220 },

            esophagus: { x: 360, y: 50 },
            omasalOrifice: { x: 710, y: 380 }
        };

        class FoodParticle {
            constructor(type, size = 'large') {
                this.type = type;
                this.size = size;
                this.x = anatomy.esophagus.x + (Math.random() - 0.5) * 30;
                this.y = anatomy.esophagus.y;
                this.stage = 'entering';
                this.fermentation = 0;
                this.radius = type === 'fiber' ? (size === 'large' ? 7.5 : 3.5) : (size === 'large' ? 5.5 : 2.8);
                this.color = type === 'fiber' ? '#10b981' : '#fbbf24';
                this.isDead = false;
            }

            update() {
                if (this.stage === 'entering') {
                    this.y += 2.5;
                    this.x += (Math.random() - 0.5) * 1.5;
                    if (this.y >= anatomy.fiberMat.yTop + 20) {
                        this.stage = (this.type === 'fiber' && this.size === 'large') ? 'mat' : 'transition';
                    }
                } else if (this.stage === 'mat') {
                    this.x += (Math.random() - 0.5) * 1.2;
                    this.y += (Math.random() - 0.5) * 0.6;
                    
                    if (this.y < anatomy.fiberMat.yTop + 10) this.y = anatomy.fiberMat.yTop + 10;
                    if (this.y > anatomy.fiberMat.yBottom - 10) this.y = anatomy.fiberMat.yBottom - 10;

                    if (state.ph > 5.8) this.fermentation += 0.15;
                    
                    if (this.fermentation > 100) {
                        this.size = 'small';
                        this.radius = 3.5;
                        this.color = '#059669';
                        this.stage = 'transition';
                    }
                } else if (this.stage === 'transition') {
                    this.x += (Math.random() - 0.5) * 1.5;
                    this.y += 0.2 + (Math.random() - 0.5) * 1.0;

                    if (this.y > anatomy.transition.yBottom) {
                        this.stage = 'liquid';
                    }
                } else if (this.stage === 'liquid') {
                    this.x += (Math.random() - 0.5) * 2;
                    this.y += (Math.random() - 0.5) * 2;

                    if (this.y < anatomy.liquidZone.yTop + 10) this.y = anatomy.liquidZone.yTop + 15;
                    if (this.y > anatomy.liquidZone.yBottom - 15) this.y = anatomy.liquidZone.yBottom - 20;

                    this.fermentation += 0.2;
                    
                    if (this.size === 'small' && Math.random() < 0.004) {
                        this.stage = 'exiting';
                    }
                } else if (this.stage === 'regurgitating') {
                    this.y -= 3.5;
                    this.x += (anatomy.esophagus.x - this.x) * 0.08;
                    if (this.y <= anatomy.esophagus.y - 20) {
                        this.stage = 'chewing';
                        this.chewTimer = 80;
                    }
                } else if (this.stage === 'chewing') {
                    this.x = anatomy.esophagus.x + (Math.random() - 0.5) * 10;
                    this.y = anatomy.esophagus.y - 20 + (Math.random() - 0.5) * 10;
                    this.chewTimer--;
                    if (this.chewTimer <= 0) {
                        this.size = 'small';
                        this.radius = 3.5;
                        this.color = '#047857';
                        this.stage = 'entering';
                        state.salivationBuffer += 0.15;
                        state.co2BufferSurge += 0.3;
                    }
                } else if (this.stage === 'exiting') {
                    this.x += (anatomy.omasalOrifice.x - this.x) * 0.08;
                    this.y += (anatomy.omasalOrifice.y - this.y) * 0.08;
                    this.radius *= 0.94;
                    if (this.radius < 0.5) this.isDead = true;
                }
            }

            draw() {
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2);
                ctx.fillStyle = this.color;
                ctx.fill();
                ctx.strokeStyle = '#0f172a';
                ctx.lineWidth = 1;
                ctx.stroke();
            }
        }

        class VFAParticle {
            constructor(type) {
                this.type = type; 
                this.x = anatomy.center.x + (Math.random() - 0.5) * 380;
                this.y = anatomy.transition.yTop + Math.random() * (anatomy.liquidZone.yBottom - anatomy.transition.yTop);
                this.vx = (Math.random() - 0.5) * 0.8;
                this.vy = (Math.random() - 0.5) * 0.8;
                this.life = 150 + Math.random() * 80;
                
                if (type === 'acetate') this.color = '#10b981';      
                else if (type === 'propionate') this.color = '#fbbf24'; 
                else if (type === 'butyrate') this.color = '#818cf8';   
                else if (type === 'lactate') this.color = '#f43f5e';    
                
                this.size = 3.2;
            }

            update() {
                this.x += this.vx;
                this.y += this.vy;
                this.life--;

                if (this.y < anatomy.transition.yTop + 10) this.vy *= -1;
                if (this.y > anatomy.liquidZone.yBottom - 10) this.vy *= -1;
            }

            draw() {
                ctx.save();
                ctx.globalAlpha = Math.min(0.85, this.life / 30);
                ctx.fillStyle = this.color;
                ctx.fillRect(this.x - this.size/2, this.y - this.size/2, this.size, this.size);
                ctx.restore();
            }
        }

        class Microbe {
            constructor(type) {
                this.type = type;
                this.vx = (Math.random() - 0.5) * 0.5;
                this.vy = (Math.random() - 0.5) * 0.5;
                this.pulsePhase = Math.random() * Math.PI * 2;
                this.angle = Math.random() * Math.PI * 2; 
                this.resetPosition();
            }

            resetPosition() {
                if (this.type === 'cellulolytic') {
                    this.cx = anatomy.center.x;
                    this.cy = (anatomy.fiberMat.yTop + anatomy.fiberMat.yBottom) / 2;
                    this.rx = 200;
                    this.ry = 40;
                } else if (this.type === 'amylolytic') {
                    this.cx = anatomy.center.x;
                    this.cy = (anatomy.transition.yTop + anatomy.liquidZone.yBottom) / 2;
                    this.rx = 210;
                    this.ry = 90;
                } else {
                    this.cx = anatomy.center.x;
                    this.cy = (anatomy.fiberMat.yTop + anatomy.liquidZone.yBottom) / 2;
                    this.rx = 210;
                    this.ry = 120;
                }
                let angle = Math.random() * Math.PI * 2;
                let radiusSq = Math.random(); 
                this.x = this.cx + Math.sqrt(radiusSq) * this.rx * Math.cos(angle);
                this.y = this.cy + Math.sqrt(radiusSq) * this.ry * Math.sin(angle);
            }

            update() {
                this.pulsePhase += 0.08;
                this.vx += (Math.random() - 0.5) * 0.25;
                this.vy += (Math.random() - 0.5) * 0.25;
                
                if(this.type === 'amylolytic') this.angle += (Math.random() - 0.5) * 0.05;

                let maxSpeed = 0.7;
                if (this.type === 'methanogen') {
                    if (state.saponinActive || state.ph < 5.5) maxSpeed = 0.12;
                    else if (state.diet === 'forage_mature') maxSpeed = 1.6;
                    else maxSpeed = 0.7;
                }

                let speed = Math.hypot(this.vx, this.vy);
                if (speed > maxSpeed) {
                    this.vx = (this.vx / speed) * maxSpeed;
                    this.vy = (this.vy / speed) * maxSpeed;
                }
                this.x += this.vx;
                this.y += this.vy;

                const dx = this.x - this.cx;
                const dy = this.y - this.cy;
                const distSq = (dx * dx) / (this.rx * this.rx) + (dy * dy) / (this.ry * this.ry);

                if (distSq > 1) {
                    this.vx -= (dx / this.rx) * 0.12;
                    this.vy -= (dy / this.ry) * 0.12;
                }
            }

            draw() {
                ctx.save();
                
                if (this.type === 'cellulolytic') {
                    ctx.beginPath();
                    ctx.arc(this.x, this.y, 2.8, 0, Math.PI * 2);
                    ctx.fillStyle = '#6ee7b7'; 
                    ctx.fill();
                    ctx.strokeStyle = '#065f46';
                    ctx.lineWidth = 1.2;
                    ctx.stroke();

                } else if (this.type === 'amylolytic') {
                    ctx.translate(this.x, this.y);
                    ctx.rotate(this.angle);
                    
                    ctx.beginPath();
                    const w = 6.5; 
                    const h = 2.8; 
                    ctx.moveTo(-w/2 + h/2, -h/2);
                    ctx.lineTo(w/2 - h/2, -h/2);
                    ctx.arc(w/2 - h/2, 0, h/2, -Math.PI/2, Math.PI/2);
                    ctx.lineTo(-w/2 + h/2, h/2);
                    ctx.arc(-w/2 + h/2, 0, h/2, Math.PI/2, -Math.PI/2);
                    ctx.closePath();
                    
                    ctx.fillStyle = '#fbbf24';
                    ctx.fill();
                    
                } else if (this.type === 'methanogen') {
                    const r = 2.0;
                    const mainColor = (state.saponinActive || state.ph < 5.5) ? '#94a3b8' : '#c084fc'; 

                    if (!state.saponinActive && state.ph >= 5.5) {
                        let baseHalo = state.diet.includes('forage') ? 4.5 : 2.5;
                        let pulse = baseHalo + Math.sin(this.pulsePhase) * (baseHalo * 0.35);
                        
                        ctx.beginPath();
                        ctx.arc(this.x, this.y, r + pulse, 0, Math.PI * 2);
                        ctx.fillStyle = state.diet === 'forage_mature' ? 'rgba(253, 224, 71, 0.45)' : 'rgba(253, 224, 71, 0.15)';
                        ctx.fill();
                    }
                    
                    ctx.beginPath();
                    ctx.arc(this.x, this.y, r, 0, Math.PI * 2);
                    ctx.fillStyle = mainColor;
                    ctx.fill();
                }
                
                ctx.restore();
            }
        }

        function initMicrobes() {
            state.microbes = [];
            for (let i = 0; i < 45; i++) state.microbes.push(new Microbe('cellulolytic'));
            for (let i = 0; i < 45; i++) state.microbes.push(new Microbe('amylolytic'));
            for (let i = 0; i < 35; i++) state.microbes.push(new Microbe('methanogen'));
        }
        initMicrobes();

        class GasBubble {
            constructor(type, x, y) {
                this.type = type;
                this.x = x;
                this.y = y;
                this.vy = - (Math.random() * 1.5 + 1.2);
                this.radius = type === 'CH4' ? 4.2 : 3.2;
                this.alpha = 0.85;
            }

            update() {
                this.y += this.vy;
                this.x += (Math.random() - 0.5) * 1.1;
                if (this.y < anatomy.gasCap.yTop + 20) this.alpha -= 0.03;
            }

            draw() {
                ctx.save();
                ctx.globalAlpha = Math.max(0, this.alpha);
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2);
                
                ctx.fillStyle = this.type === 'CH4' ? 'rgba(253, 224, 71, 0.35)' : 'rgba(56, 189, 248, 0.35)';
                ctx.fill();
                ctx.strokeStyle = this.type === 'CH4' ? 'rgba(253, 224, 71, 0.8)' : 'rgba(56, 189, 248, 0.8)';
                ctx.lineWidth = 1;
                ctx.stroke();
                
                ctx.restore();
            }
        }

        function drawAnatomy() {
            ctx.lineJoin = 'round';
            ctx.lineCap = 'round';

            const cx = anatomy.center.x;

            // CAPA 4: FASE LÍQUIDA Y SEDIMENTOS
            let gradLiquid = ctx.createLinearGradient(0, anatomy.liquidZone.yTop, 0, anatomy.liquidZone.yBottom);
            gradLiquid.addColorStop(0, 'rgba(120, 53, 15, 0.7)');
            gradLiquid.addColorStop(1, 'rgba(69, 26, 3, 0.95)');
            ctx.beginPath();
            ctx.ellipse(cx, 390, anatomy.liquidZone.rx, 70, 0, 0, Math.PI * 2);
            ctx.fillStyle = gradLiquid;
            ctx.fill();
            ctx.strokeStyle = state.severeAcidosis ? '#e11d48' : (state.acidosis ? '#f43f5e' : '#475569');
            ctx.lineWidth = 3.5;
            ctx.stroke();

            // CAPA 3: ZONA DE TRANSICIÓN / SLURRY
            let gradTrans = ctx.createLinearGradient(0, anatomy.transition.yTop, 0, anatomy.transition.yBottom);
            gradTrans.addColorStop(0, 'rgba(180, 83, 9, 0.4)');
            gradTrans.addColorStop(1, 'rgba(120, 53, 15, 0.6)');
            ctx.beginPath();
            ctx.ellipse(cx, 280, anatomy.transition.rx, 55, 0, 0, Math.PI * 2);
            ctx.fillStyle = gradTrans;
            ctx.fill();

            // CAPA 2: BALSA DE FIBRA / MAT FIBROSO
            let gradMat = ctx.createLinearGradient(0, anatomy.fiberMat.yTop, 0, anatomy.fiberMat.yBottom);
            gradMat.addColorStop(0, 'rgba(16, 185, 129, 0.35)');
            gradMat.addColorStop(1, 'rgba(5, 150, 105, 0.15)');
            ctx.beginPath();
            ctx.ellipse(cx, 180, anatomy.fiberMat.rx, 50, 0, 0, Math.PI * 2);
            ctx.fillStyle = gradMat;
            ctx.fill();
            ctx.strokeStyle = 'rgba(16, 185, 129, 0.5)';
            ctx.lineWidth = 2;
            ctx.stroke();

            // CAPA 1: BÓVEDA DE GAS / CÚPULA DORSAL
            let gradGas = ctx.createLinearGradient(0, anatomy.gasCap.yTop, 0, anatomy.gasCap.yBottom);
            gradGas.addColorStop(0, 'rgba(56, 189, 248, 0.25)');
            gradGas.addColorStop(1, 'rgba(234, 179, 8, 0.05)');
            ctx.beginPath();
            ctx.ellipse(cx, 90, anatomy.gasCap.rx, 45, 0, 0, Math.PI * 2);
            ctx.fillStyle = gradGas;
            ctx.fill();
            ctx.strokeStyle = 'rgba(56, 189, 248, 0.4)';
            ctx.lineWidth = 1.5;
            ctx.stroke();

            // CARDIA Y ORIFICIO OMASAL
            ctx.beginPath();
            ctx.arc(anatomy.esophagus.x, anatomy.esophagus.y, 12, 0, Math.PI * 2);
            ctx.fillStyle = 'rgba(244, 63, 94, 0.4)';
            ctx.fill();
            ctx.strokeStyle = '#f43f5e';
            ctx.stroke();

            ctx.beginPath();
            ctx.arc(anatomy.omasalOrifice.x, anatomy.omasalOrifice.y, 14, 0, Math.PI * 2);
            ctx.fillStyle = 'rgba(14, 165, 233, 0.4)';
            ctx.fill();
            ctx.strokeStyle = '#0ea5e9';
            ctx.stroke();

            function drawLabel(text, x, y, textColor) {
                ctx.font = 'bold 12px sans-serif';
                let textWidth = ctx.measureText(text).width;
                ctx.fillStyle = 'rgba(15, 23, 42, 0.85)';
                ctx.fillRect(x - textWidth / 2 - 8, y - 13, textWidth + 16, 20);
                ctx.fillStyle = textColor;
                ctx.textAlign = 'center';
                ctx.fillText(text, x, y + 2);
            }

            drawLabel('1. Bóveda de Gas (CO₂ / CH₄)', cx, 85, '#38bdf8');
            drawLabel('2. Balsa de Fibra / Mat Flotante', cx, 175, '#34d399');
            drawLabel('3. Zona de Transición / Slurry', cx, 275, '#fbbf24');
            drawLabel('4. Fase Líquida y AGVs (Saco Ventral)', cx, 395, '#f59e0b');
            
            drawLabel('Entrada Cardias', anatomy.esophagus.x, anatomy.esophagus.y - 20, '#fca5a5');
            drawLabel('Salida Omaso', anatomy.omasalOrifice.x, anatomy.omasalOrifice.y + 25, '#7dd3fc');
        }

        function updateVFAParticles() {
            const targetCount = 68;
            if (state.vfaParticles.length < targetCount) {
                const rand = Math.random() * 100;
                let type = 'acetate';
                
                const propThresh = state.acetate;
                const butyThresh = propThresh + state.propionate;
                const lactThresh = butyThresh + state.butyrate;

                if (rand > lactThresh) type = 'lactate';
                else if (rand > butyThresh) type = 'butyrate';
                else if (rand > propThresh) type = 'propionate';

                state.vfaParticles.push(new VFAParticle(type));
            }

            for (let i = state.vfaParticles.length - 1; i >= 0; i--) {
                const vfa = state.vfaParticles[i];
                vfa.update();
                vfa.draw();

                if (vfa.life <= 0) {
                    state.vfaParticles.splice(i, 1);
                }
            }
        }

        // --- CÁLCULO ESTEQUIOMÉTRICO MODELADO DE GASES ---
        function updateGasKinetics() {
            const apRatio = state.acetate / Math.max(1, state.propionate); 
            
            let methanogenInhibitionFactor = state.saponinActive ? 0.08 : 1.0;
            if (state.diet === 'diet_lipids') methanogenInhibitionFactor *= 0.45; // Biohidrogenación suprime CH4
            if (state.ph < 5.5) methanogenInhibitionFactor *= 0.25; // pH ácido deprime metanogénicas

            state.ch4RateLDay = Math.round((70 + (apRatio * 75)) * methanogenInhibitionFactor);
            
            let baseCo2Rate = 420 + (state.acetate * 2.5) + (state.lactate * 10) + (state.diet === 'concentrate_acidosis' ? 160 : 0);
            if (state.co2BufferSurge > 0) {
                baseCo2Rate += state.co2BufferSurge * 250;
                state.co2BufferSurge -= 0.005;
            }
            state.co2RateLDay = Math.round(baseCo2Rate);

            const scaleFactor = 0.00008;
            state.ch4AccumulatedL += state.ch4RateLDay * scaleFactor;
            state.co2AccumulatedL += state.co2RateLDay * scaleFactor;

            if (Math.random() < (state.ch4RateLDay / 3000)) {
                const x = anatomy.center.x + (Math.random() - 0.5) * 300;
                const y = anatomy.transition.yTop + Math.random() * 100;
                state.bubbles.push(new GasBubble('CH4', x, y));
            }
            if (Math.random() < (state.co2RateLDay / 2500)) {
                const x = anatomy.center.x + (Math.random() - 0.5) * 350;
                const y = anatomy.fiberMat.yTop + Math.random() * 150;
                state.bubbles.push(new GasBubble('CO2', x, y));
            }
        }

        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);

            drawAnatomy();
            updateVFAParticles();

            state.microbes.forEach(m => {
                m.update();
                m.draw();
            });

            for (let i = state.particles.length - 1; i >= 0; i--) {
                const p = state.particles[i];
                p.update();
                p.draw();

                if (p.isDead) {
                    state.particles.splice(i, 1);
                }
            }

            for (let i = state.bubbles.length - 1; i >= 0; i--) {
                const b = state.bubbles[i];
                b.update();
                b.draw();

                if (b.alpha <= 0) state.bubbles.splice(i, 1);
            }

            updatePhysiology();
            updateMicrobialPopulations();
            updateGasKinetics();
            requestAnimationFrame(animate);
        }

        function updatePhysiology() {
            let targetPh = 6.65;
            if (state.diet === 'forage_mature') targetPh = 6.95;
            else if (state.diet === 'forage_young') targetPh = 6.75;
            else if (state.diet === 'mixed') targetPh = 6.55;
            else if (state.diet === 'concentrate_cereal') targetPh = 5.65;
            else if (state.diet === 'concentrate_acidosis') targetPh = 5.10;
            else if (state.diet === 'diet_lipids') targetPh = 6.40;
            else if (state.diet === 'diet_npn') targetPh = 6.60;

            if (state.salivationBuffer > 0) {
                targetPh += 0.4;
                state.salivationBuffer -= 0.001;
            }

            state.ph += (targetPh - state.ph) * 0.005;
            state.acidosis = state.ph < 5.8;
            state.severeAcidosis = state.ph < 5.25;

            let targetAce = 62, targetProp = 26, targetBut = 12, targetLac = 0;

            if (state.diet === 'forage_mature') {
                targetAce = 72; targetProp = 16; targetBut = 12; targetLac = 0;
            } else if (state.diet === 'forage_young') {
                targetAce = 65; targetProp = 22; targetBut = 13; targetLac = 0;
            } else if (state.diet === 'mixed') {
                targetAce = 60; targetProp = 26; targetBut = 13; targetLac = 1;
            } else if (state.diet === 'concentrate_cereal') {
                targetAce = 45; targetProp = 38; targetBut = 12; targetLac = 5;
            } else if (state.diet === 'concentrate_acidosis') {
                targetAce = 28; targetProp = 34; targetBut = 10; targetLac = 28;
            } else if (state.diet === 'diet_lipids') {
                targetAce = 54; targetProp = 33; targetBut = 13; targetLac = 0;
            } else if (state.diet === 'diet_npn') {
                targetAce = 58; targetProp = 28; targetBut = 14; targetLac = 0;
            }

            state.acetate += (targetAce - state.acetate) * 0.01;
            state.propionate += (targetProp - state.propionate) * 0.01;
            state.butyrate += (targetBut - state.butyrate) * 0.01;
            state.lactate += (targetLac - state.lactate) * 0.01;

            if (state.saponinActive) {
                state.saponinTimer--;
                if (state.saponinTimer <= 0) state.saponinActive = false;
            }

            document.getElementById('phValue').innerText = state.ph.toFixed(2);
            document.getElementById('phBar').style.width = `${Math.min(100, Math.max(0, (state.ph - 5.0) / 2.2 * 100))}%`;

            if (state.severeAcidosis) {
                document.getElementById('phValue').className = 'text-xl md:text-2xl font-bold text-rose-600 animate-bounce';
                document.getElementById('statusBadge').innerText = 'CRÍTICO: Acidosis Ruminal Láctica Aguda';
                document.getElementById('statusBadge').className = 'px-3 py-1.5 md:px-4 md:py-2 rounded-full text-xs font-semibold bg-rose-600/30 text-rose-300 border border-rose-500/50';
            } else if (state.acidosis) {
                document.getElementById('phValue').className = 'text-xl md:text-2xl font-bold text-rose-400 animate-pulse';
                document.getElementById('statusBadge').innerText = 'ALERTA: Acidosis Subaguda (SARA)';
                document.getElementById('statusBadge').className = 'px-3 py-1.5 md:px-4 md:py-2 rounded-full text-xs font-semibold bg-rose-500/20 text-rose-400 border border-rose-500/30';
            } else {
                document.getElementById('phValue').className = 'text-xl md:text-2xl font-bold text-emerald-400';
                document.getElementById('statusBadge').innerText = 'Estado: Fisiológico Normal';
                document.getElementById('statusBadge').className = 'px-3 py-1.5 md:px-4 md:py-2 rounded-full text-xs font-semibold bg-emerald-500/20 text-emerald-300 border border-emerald-500/30';
            }

            document.getElementById('acetatePct').innerText = `${Math.round(state.acetate)}%`;
            document.getElementById('acetateBar').style.width = `${state.acetate}%`;
            document.getElementById('propionatePct').innerText = `${Math.round(state.propionate)}%`;
            document.getElementById('propionateBar').style.width = `${state.propionate}%`;
            document.getElementById('butyratePct').innerText = `${Math.round(state.butyrate)}%`;
            document.getElementById('butyrateBar').style.width = `${state.butyrate}%`;
            document.getElementById('lactatePct').innerText = `${Math.round(state.lactate)}%`;
            document.getElementById('lactateBar').style.width = `${Math.min(100, state.lactate * 3.2)}%`;

            document.getElementById('ch4RateDisplay').innerText = `${state.ch4RateLDay} L/día`;
            document.getElementById('ch4TotalDisplay').innerText = `Acumulado: ${state.ch4AccumulatedL.toFixed(1)} L`;
            
            document.getElementById('co2RateDisplay').innerText = `${state.co2RateLDay} L/día`;
            document.getElementById('co2TotalDisplay').innerText = `Acumulado: ${state.co2AccumulatedL.toFixed(1)} L`;

            document.getElementById('cellulolyticCount').innerText = state.diet.includes('forage') ? 'Cocáceas (Fibra)' : (state.ph < 5.8 ? 'Deprimida / Lisis' : 'Moderada');
            document.getElementById('cellulolyticCount').className = `font-mono font-bold ${state.ph < 5.8 ? 'text-rose-400' : 'text-emerald-400'}`;

            document.getElementById('amylolyticCount').innerText = state.diet.includes('concentrate') ? 'Bacilos Proliferando' : 'Población Normal';
            document.getElementById('amylolyticCount').className = `font-mono font-bold ${state.diet.includes('concentrate') ? 'text-amber-400' : 'text-amber-500'}`;

            document.getElementById('methanogenStatus').innerText = state.saponinActive ? 'Inhibidas (Saponinas)' : (state.diet === 'diet_lipids' ? 'Suprimidas (Lípidos)' : (state.ph < 5.5 ? 'Inhibidas (Acidez)' : 'Activas'));
            document.getElementById('methanogenStatus').className = `font-mono font-bold ${(state.saponinActive || state.diet === 'diet_lipids' || state.ph < 5.5) ? 'text-slate-400' : 'text-purple-400'}`;
        }

        function updateMicrobialPopulations() {
            let targetCellulolytic = 45;
            let targetAmylolytic = 45;
            let targetMethanogen = 35;

            if (state.diet === 'forage_mature') {
                targetCellulolytic = 85; targetAmylolytic = 15; targetMethanogen = 50;
            } else if (state.diet === 'forage_young') {
                targetCellulolytic = 70; targetAmylolytic = 30; targetMethanogen = 40;
            } else if (state.diet === 'mixed') {
                targetCellulolytic = 45; targetAmylolytic = 45; targetMethanogen = 35;
            } else if (state.diet === 'concentrate_cereal') {
                targetCellulolytic = 20; targetAmylolytic = 80; targetMethanogen = 20;
            } else if (state.diet === 'concentrate_acidosis') {
                targetCellulolytic = 5; targetAmylolytic = 100; targetMethanogen = 10;
            } else if (state.diet === 'diet_lipids') {
                targetCellulolytic = 35; targetAmylolytic = 40; targetMethanogen = 12;
            } else if (state.diet === 'diet_npn') {
                targetCellulolytic = 50; targetAmylolytic = 60; targetMethanogen = 30;
            }

            if (state.saponinActive) targetMethanogen = 5;
            if (state.ph < 5.5) {
                targetCellulolytic = Math.min(targetCellulolytic, 5);
                targetMethanogen = Math.min(targetMethanogen, 8);
            }

            let currentCellulolytic = 0, currentAmylolytic = 0, currentMethanogen = 0;
            for (let m of state.microbes) {
                if (m.type === 'cellulolytic') currentCellulolytic++;
                else if (m.type === 'amylolytic') currentAmylolytic++;
                else if (m.type === 'methanogen') currentMethanogen++;
            }

            const changeRate = 0.2;

            if (currentCellulolytic < targetCellulolytic && Math.random() < changeRate) state.microbes.push(new Microbe('cellulolytic'));
            else if (currentCellulolytic > targetCellulolytic && Math.random() < changeRate) {
                const idx = state.microbes.findIndex(m => m.type === 'cellulolytic');
                if (idx > -1) state.microbes.splice(idx, 1);
            }

            if (currentAmylolytic < targetAmylolytic && Math.random() < changeRate) state.microbes.push(new Microbe('amylolytic'));
            else if (currentAmylolytic > targetAmylolytic && Math.random() < changeRate) {
                const idx = state.microbes.findIndex(m => m.type === 'amylolytic');
                if (idx > -1) state.microbes.splice(idx, 1);
            }

            if (currentMethanogen < targetMethanogen && Math.random() < changeRate) state.microbes.push(new Microbe('methanogen'));
            else if (currentMethanogen > targetMethanogen && Math.random() < changeRate) {
                const idx = state.microbes.findIndex(m => m.type === 'methanogen');
                if (idx > -1) state.microbes.splice(idx, 1);
            }
        }

        // Event Listeners
        document.getElementById('dietSelect').addEventListener('change', (e) => {
            state.diet = e.target.value;
        });

        document.getElementById('feedBtn').addEventListener('click', () => {
            const count = 15;
            for (let i = 0; i < count; i++) {
                setTimeout(() => {
                    let type = 'fiber';
                    if (state.diet.includes('forage')) type = 'fiber';
                    else if (state.diet.includes('concentrate')) type = 'starch';
                    else if (state.diet === 'diet_npn') type = Math.random() > 0.3 ? 'starch' : 'fiber';
                    else type = Math.random() > 0.5 ? 'fiber' : 'starch';

                    state.particles.push(new FoodParticle(type, 'large'));
                }, i * 100);
            }
        });

        document.getElementById('ruminateBtn').addEventListener('click', () => {
            const matParticles = state.particles.filter(p => p.stage === 'mat' || p.stage === 'transition' || p.stage === 'liquid');
            if (matParticles.length > 0) {
                const numToRegurgitate = Math.min(5, matParticles.length);
                for (let i = 0; i < numToRegurgitate; i++) {
                    const p = matParticles[Math.floor(Math.random() * matParticles.length)];
                    p.stage = 'regurgitating';
                }
            }
        });

        document.getElementById('bufferBtn').addEventListener('click', () => {
            state.salivationBuffer += 0.5;
            state.co2BufferSurge += 1.2;
        });

        document.getElementById('saponinBtn').addEventListener('click', () => {
            state.saponinActive = true;
            state.saponinTimer = 600;
        });

        document.getElementById('resetBtn').addEventListener('click', () => {
            state.diet = 'mixed';
            document.getElementById('dietSelect').value = 'mixed';
            state.ph = 6.65;
            state.acetate = 62;
            state.propionate = 26;
            state.butyrate = 12;
            state.lactate = 0;
            state.ch4RateLDay = 280;
            state.co2RateLDay = 540;
            state.ch4AccumulatedL = 0;
            state.co2AccumulatedL = 0;
            state.saponinActive = false;
            state.salivationBuffer = 0;
            state.co2BufferSurge = 0;
            state.particles = [];
            state.vfaParticles = [];
            state.bubbles = [];
            initMicrobes();
        });

        window.onload = function() {
            animate();
        };
    </script>
</body>
</html>
